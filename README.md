# first run npm i command inside dotNet folder

# run npm start

# you can test api by changing value of country and forecastType - http://localhost:5000/weatherAPI?country=china&&forecastType=forecast
